[package]
name = "mds_db"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
rusqlite_migration = "1"
thiserror = "1"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
